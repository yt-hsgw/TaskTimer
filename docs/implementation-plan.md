# 実装計画

## Phase 0: 設計ベースライン

成果物:

- MVP仕様。
- アーキテクチャ資料。
- ドメインモデル。
- データベーススキーマ案。
- セキュリティ設計。
- レビューチェックリスト。
- GitHubテンプレートとリポジトリ設定。

完了条件:

- 設計レビューが完了している。
- `docs/mvp-spec.md` の未決事項が解決済み、または明示的に延期されている。

## Phase 1: プロジェクトスキャフォールド

成果物:

- Tauriプロジェクト構成。
- React + TypeScriptフロントエンド。
- Rust command境界。
- SQLiteマイグレーション実行器。
- 依存関係レビュー。

完了条件:

- macOS/Windowsの開発環境でアプリが起動する。
- 設定上、アプリ実行時の外部通信が無効化されている。
- 空データベースを初期化できる。
- 未決事項がプロダクト判断として反映されている。

現在のスキャフォールド状態:

- Tauri、Vite、React、TypeScriptのファイルは配置済み。
- Rust command境界はヘルスチェック、カレンダー範囲取得、アクティブタイマー取得、通知表示設定取得、タスク/サブタスク作成、タイマー開始/停止まで配置済み。
- SQLite初期マイグレーションとRepository実装は配置済み。
- `CreateTask`、`CreateSubtask`、`StartTimer`、`StopActiveTimer` はApplication Use Case経由で実行し、入力検証とトランザクション境界を持つ。
- タスク/サブタスク一覧は上限付きの読み取りRepositoryから取得し、UIはモックデータではなくSQLite由来の状態を表示する。
- 実行時HTTP、自動更新、分析、リモートアセット機能は設定していない。

## Phase 2: ドメインと永続化

成果物:

- Taskドメインモデル。
- Subtaskドメインモデル。
- TimerSessionドメインモデル。
- SQLite Repository。
- Application Use Case。初回実装スライスで `GetPomodoroSettings`、`UpdatePomodoroSettings`、`GetActivePomodoro`、`StartPomodoro` まで追加済み。

完了条件:

- ドメインユニットテストが通る。
- SQLite Repositoryテストが通る。
- 単一アクティブタイマー制約がApplicationとDBの両方で守られる。

設計判断:

- Use Caseは未検証入力を検証済みコマンドへ変換する境界として実装する。
- SQLite実装は1 Use Caseにつき1トランザクションを開始し、親存在確認、状態確認、タイマー作成/停止、対象状態更新を同一トランザクションに閉じる。
- OS通知などの副作用は今回のUse Caseには含めず、DBコミット後副作用として後続Issueで扱う。

トレードオフ:

- Tauri commandから直接SQLを書かず保守性を優先する一方、Application層の型とDTOが増える。
- `tasks` と `subtasks` を分けているため作成処理は重複するが、親タスクとサブタスクの意味を明確に保てる。

## Phase 3: コアUI

成果物:

- タスクリスト。
- タスク詳細。
- サブタスク編集。
- アクティブタイマー操作。
- カレンダー画面。

完了条件:

- ユーザーがMVP受け入れ条件を手動で完走できる。
- メモ本文をHTMLとして描画しない。

現在の到達点:

- タスク作成、サブタスク作成、タイマー開始/停止ボタンはTauri command経由でApplication Use Caseへ接続済み。
- カレンダーはSQLiteの日付範囲クエリ結果を表示し、週/日/月ビューを切り替えられる。
- タスク一覧とサブタスク一覧はSQLiteの読み取りRepository結果を表示し、アプリ再起動後も保存済みデータを読める。
- 親タスク完了時の確認、サブタスク完了、タスク/サブタスク削除はApplication Use Caseへ接続済み。
- タイトル、期限、目標時間、繰り返し、メモの編集は右詳細ペインへ実装済み。
- アーカイブ操作は後続Issue #52で扱う。

設計判断:

- PresentationはSQLiteやトランザクションを直接扱わず、Tauri Gateway経由でApplication commandを呼ぶ。
- タスク一覧はMVPの安全上限として200件に制限し、カレンダーは日付範囲で絞り込む。
- メモ本文はReactの通常テキスト描画と `white-space: pre-wrap` で表示し、HTMLとして描画しない。
- 未完了サブタスクがある親タスクの完了確認はUIで行い、Application Use Caseでも確認フラグを検証する。
- 削除時は関連タイマー履歴と通知ルールを同一トランザクションでソフト削除し、開始中タイマーを通常検索から除外する。

代替案:

UI専用にすべてのタスク、サブタスク、タイマー履歴を一括ロードする案もある。

利点:

- フロント側で自由に集計しやすい。

欠点:

- データ増加時に初期表示が重くなり、タイマー操作の応答性も落ちやすい。

決定: MVPではタスクツリーの上限付き読み取りとカレンダーの範囲読み取りを分ける。

## Phase 4: 通知

成果物:

- 通知ルールUI。
- OS通知アダプター。
- 登録状態と再試行処理。

完了条件:

- macOS/Windowsでローカル通知が動く。
- 通知登録失敗がUIで分かり、再試行できる。

現在の到達点:

- 通知表示モードはSQLiteへ保存できる。
- タスク/サブタスク作成時に開始予定日・期限から通知ルールを作成する。
- 期限到来済み通知ルールはTauri notification plugin経由でOS通知へ送信する。
- 送信成功時は `registered`、失敗時は `failed` と `last_error` を保存する。
- 設定画面で通知全体をON/OFFできる。
- 設定画面でdispatch結果と失敗メッセージを確認でき、再試行できる。
- 通知送信の成功/失敗は `notification_delivery_attempts` に保存し、設定画面で最新履歴を確認できる。
- アプリ起動中は、最も近い未来の `pending` / `failed` 通知を読み取り、React側の単一ローカルタイマーから既存 `dispatch_due_notifications` を呼び出せる。
- 起動、復帰、通知設定変更、タスク/サブタスク期限変更、バックアップ復元後は `sync_notifications` を通知再同期入口として使い、期限到来dispatch後に次回通知予定を再予約する。

設計判断:

- `generic` モードでは対象タイトルをOS通知adapterへ渡さない。
- メモ本文は通知本文に含めない。
- 通知全体OFF時は通知ルールを変更せず、dispatch入口でOS通知送信を止める。
- 将来時刻通知は段階導入する。第1段階ではアプリ起動中のローカルスケジューラで既存 `dispatch_due_notifications` を呼び、第2段階ではWindows/macOSネイティブ永続登録を検証する。
- `sync_notifications` は新しいOS永続登録状態を持たず、DB上の `notification_rules` からローカルタイマーを再生成する。

## Phase 5: リリース前強化

成果物:

- パッケージング。
- 手動デスクトップテスト記録。
- リリースノート。
- 実行時通信チェック。

完了条件:

- macOS/Windowsの手動確認が通る。
- タスクタイトルやメモ本文がログに出ない。
- リリース成果物を生成できる。

## Phase 6: UI/UX再設計

成果物:

- UI/UX再設計仕様。
- 左ナビゲーションペイン。
- 中央タスク一覧ビュー。
- 右タスク詳細ペイン。
- カレンダービュー移管。
- 設定ビュー移管。
- タスク一覧Read Model。
- お気に入り、完了セクション、サブタスク進捗表示。
- レスポンシブ幅ごとのレイアウト。

完了条件:

- [UI/UX再設計仕様](ui-ux-redesign.md) の受け入れ条件を満たす。
- 既存のタスク/サブタスク/タイマー/通知Use Case境界をUIへ漏らさない。
- 左ペインの `Ctrl+B` とタスク追加の `Ctrl+N` が動作する。
- カレンダーと通知設定が新しいビューへ移管される。
- 768px未満でも、一覧、詳細、設定、カレンダーの主要操作が破綻しない。
- 今日ビュー、メモプレビュー、ペイン内スクロールが現行UIへ反映されている。

設計判断:

- 実装はデータモデルとRead Modelから始める。
- Microsoft To Doの画面構造は参考にするが、色、タイマー体験、カレンダー体験はTaskTimer独自にする。
- 右ペインは基本非表示にし、タスク選択時に詳細を開く。
- タイマー一時停止/再開と繰り返し設定は実装済み。リスト管理は #54、アーカイブは #52、UI設定永続化は #57 で継続する。

推奨分割:

1. データモデルとRead Modelを拡張する。
2. 左ナビゲーションとApp Shellを作る。
3. 中央タスク一覧を新仕様へ置き換える。
4. 右詳細ペインへ詳細操作を移管する。
5. カレンダーと設定をビューとして移管する。
6. タイマー一時停止/再開、繰り返し、通知全体設定を追加する。完了済み。
7. 今日ビュー、メモプレビュー、ペイン内スクロールを現行UIへ反映する。完了済み。
8. カスタムリスト管理、アーカイブ、UI設定永続化、大量データ性能検証、バックアップ方針を後続Issueで進める。

危険ケース:

- UIだけ先に作り、リスト、お気に入り、タイマー一時停止の正がPresentationに寄る。
- 完了済みタスク移動や進捗バーが、DB状態と不一致になる。
- 右ペインの削除操作が狭い画面で誤操作される。
- カレンダー改修時に既存の範囲クエリが全件読み込みへ退化する。

## Phase 7: ポモドーロタイマー

成果物:

- ポモドーロ設計資料。
- ポモドーロ設定。
- ポモドーロセッションのドメインモデル。
- SQLite Repositoryとマイグレーション。
- Application Use Case。
- 右詳細ペインの通常タイマー/ポモドーロ切替。
- 作業終了/休憩終了通知。
- バックアップ、JSON、CSVエクスポート対応。

完了条件:

- 通常タイマーは既存どおり使える。
- ポモドーロの作業フェーズが実作業時間として保存される。
- 休憩フェーズは作業時間履歴に含まれない。
- 通常タイマーとポモドーロを合わせてアプリ全体で同時に1件だけ進行する。
- アプリ再起動後に進行中フェーズを復元できる。
- 通知設定に従って作業終了/休憩終了通知を送れる。
- アプリ実行時の外部通信や新しいTauri権限を追加しない。

設計判断:

- `timer_sessions` は作業時間履歴の正として維持する。
- 休憩フェーズは `timer_sessions` へ保存せず、ポモドーロ用の状態として扱う。
- DB更新はApplication Use Caseを入口とし、作業フェーズ完了時は `timer_sessions` 確定とポモドーロ状態更新を同一トランザクションで扱う。

推奨分割:

1. データモデル、Use Case、Repository境界を追加する。完了。
2. SQLiteマイグレーションとRustテストを追加する。完了。
3. 右詳細ペインに通常/ポモドーロ切替と進行中表示を追加する。
4. 設定画面にポモドーロ既定値を追加する。
5. 作業終了/休憩終了通知と復帰時再同期を追加する。
6. バックアップ、JSON、CSVエクスポートを更新する。完了。
7. 大量データ計測を更新する。

危険ケース:

- 休憩時間を作業時間として保存してしまう。
- 作業終了と休憩開始の境界で二重にタイマー履歴が作られる。
- ポモドーロ休憩中に別タスクの通常タイマーを開始できる。
- 復帰時再同期で作業終了通知や休憩終了通知が重複する。
